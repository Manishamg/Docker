pipeline{
    agent any
    stages{
        stage('Checkout') {
            steps {
                script {
                    checkout scm
                }
            }}
        stage ('docker images'){
            steps{
                sh "su - jenkins"
                sh 'yum install -y git'
            }
        }
    }
post{
    always{
     echo 'successssssssssssss'
}
}
}
